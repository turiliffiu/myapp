# =============================================
# AMBIENTE
# =============================================
DEBUG=True
# True  = development (log verbosi, errori dettagliati)
# False = production  (sicurezza HTTPS attiva, log minimo)

# =============================================
# SICUREZZA
# =============================================
SECRET_KEY=sostituisci_con_una_chiave_generata_da_Django_minimo_50_caratteri
# Genera: python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

ALLOWED_HOSTS=localhost,127.0.0.1
# In produzione: il tuo dominio reale, es: myapp.tuodominio.com

# =============================================
# DATABASE
# =============================================
# Per SQLite (development):
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASS=
DB_HOST=
DB_PORT=

# Per PostgreSQL (production):
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=myapp_db
# DB_USER=myapp_user
# DB_PASS=changeme
# DB_HOST=localhost
# DB_PORT=5432

# =============================================
# UPLOAD FILE (opzionale)
# =============================================
UPLOAD_MAX_SIZE_MB=10
# CSRF Trusted Origins (comma-separated list for HTTPS domains)
CSRF_TRUSTED_ORIGINS=
